import { SiteShell } from '@/components/site-shell'
import { saveProfile } from './actions'
export default async function Onboarding({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <SiteShell><main className="form card"><div className="eyebrow">Profile setup</div><h1>Tell Bandhan who you are.</h1>{error && <p className="notice">{error}</p>}<form action={saveProfile}><div className="field"><label htmlFor="displayName">Display name</label><input id="displayName" name="displayName" minLength={2} maxLength={60} required /></div><div className="field"><label htmlFor="city">City</label><input id="city" name="city" maxLength={80} /></div><div className="field"><label htmlFor="role">I want to</label><select id="role" name="role"><option value="customer">Discover people and request bookings</option><option value="provider">Offer event companionship services</option></select></div><button className="button accent">Save profile</button></form><p className="meta">Provider status is not a verification badge. Establish verification, conduct, and moderation processes before operating publicly.</p></main></SiteShell> }
