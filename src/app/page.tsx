import Link from 'next/link'
import { SiteShell } from '@/components/site-shell'
import { featuredProviders } from '@/lib/demo-data'
export default function Home() { return <SiteShell><main><section className="hero"><div className="shell"><div className="eyebrow">Made in India · safety-first</div><h1>Meaningful company, on your terms.</h1><p>Bandhan brings genuine soulmate discovery and respectful event companionship into one thoughtful, inclusive experience.</p><div className="actions"><Link className="button accent" href="/discover">Explore people</Link><Link className="button ghost" href="/auth">Create your profile</Link></div></div></section><section className="shell section"><h2>Meet the community</h2><p>These are clearly marked sample profiles until your Supabase project is configured.</p><div className="grid">{featuredProviders.map((person) => <article className="card" key={person.name}><div className="avatar">{person.initials}</div><h3>{person.name}</h3><div className="meta">{person.city}</div><div className="pill">{person.service}</div><p><strong>{person.price}</strong></p><Link className="button ghost" href="/auth">Request a booking</Link></article>)}</div></section><section className="shell notice">Demo note: identity checks, payment collection, and verification badges are not active in V4. Connect approved providers and credentials before offering them.</section></main></SiteShell> }
